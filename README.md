# keraformer

Modular transformer building blocks for Keras 3.

This repository is being implemented in phases, starting with the package bootstrap and moving module by module through masks, embeddings, normalization, attention, feedforward blocks, composite blocks, models, optimizers, losses, and utilities.
